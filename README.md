A small Python utility that searches environment variables using
case-insensitive pattern matching and prints any matching keys and values.
Useful for quickly checking exported API keys and other environment settings.
