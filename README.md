## Environment Key Finder

A small Python utility that searches environment variables using case-insensitive pattern matching and prints any matching keys and values. You don’t need to type the full variable name , for example, typing `google` will match `GOOGLE_API_KEY`, and typing `api` will list all API-related keys.
